FROM node:22

# Carpeta de trabajo en el contenedor
WORKDIR /app

# Angular CLI global
RUN npm install -g @angular/cli

# Exponer puerto 4200 para Angular
EXPOSE 4200

# Ejecutar ng serve sobre tu código montado
CMD ["ng", "serve", "--host", "0.0.0.0", "--port", "4200"]
